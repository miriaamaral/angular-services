export const environment = {
  production: false,
  // URL base da API de pokémons (exemplo):
  pokeApi: 'https://pokeapi.co/api/v2'
};

// environment em pt-br significa: ambiente
// A pasta `environments` contém arquivos de configuração para diferentes ambientes,
// como desenvolvimento (`environment.ts`) e produção (`environment.prod.ts`).
// Aqui mantemos apenas as chaves necessárias para o exemplo atual.
